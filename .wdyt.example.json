{
  "//": "Copy to .wdyt.json. Every field is optional.",

  "backend": "auto",

  "models": ["openai/gpt-5.1", "google/gemini-3-pro"],

  "budget": 140000,
  "head_turns": 6,
  "temperature": 0.7,
  "timeout": 600,

  "//backends": "Override a CLI backend's command line here when an upstream flag changes, instead of patching the script. 'tail' is appended after the model flag, for a stdin sentinel or a variadic list.",
  "backends": {
    "codex": {
      "cmd": ["codex", "exec", "--sandbox", "read-only", "--ask-for-approval", "never"],
      "tail": ["-"],
      "model_flag": "-m",
      "models": []
    },
    "gemini": {
      "cmd": ["gemini"],
      "tail": [],
      "model_flag": "-m"
    }
  }
}
